create your first line item (Fix order_dialog.rb:106!!)
http://guides.rubyonrails.org/association_basics.html
http://edgeguides.rubyonrails.org/active_record_migrations.html

Intentar que line_item_dialog devuelva un to_hash que se pueda utilizar como
en main.cpp import_products_as_csv donde se crean objetos pasando un hash y no teniendo que especificar cada attributo. Muy util cuando se refactoriza!!

Run bundle install to generate ctags and load them in sublime

continue with order dialog. Use combobox with data everywhere (refactor!!)
Find out how FXDataTarget works with custom data.

Small improvements
Columns can be manually extended, but extend automatically based on content?
Order products when header is pressed
Hide columns id (hide them and don't delete them since they are needed to get objects)
Validate csv files when importing 
If orders table is empty, presing the header causes a crash